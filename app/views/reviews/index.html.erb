these are all your reviews <%= current_user.username %><br>

<% current_user.reviews.each do |review| %>
    <%= link_to "#{review.book.title}", book_path(review.book) %><br>
<%end%>
<%= link_to "<< back to index", books_path %>