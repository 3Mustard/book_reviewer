show page for <%= @book.title %><br>

<% if @review != nil %>
    <%= @review.rating %>
    <%= @review.content %>
<%else%>
    <%= link_to "Add a Review", new_book_review_path(@book) %> is not sending @book. 
<%end%>
<br>
<%= link_to "<< back to index", books_path %>